# MacroVision Web MVP v0.8 Route/API Map

This map covers the nine planned Persian MVP routes. All API paths are existing
MacroVision v0.7.0 contracts unless the row explicitly identifies a gap. Public
availability in this document means product intent, not current authorization: the
existing API has no authentication and must remain on a trusted private network.

| Web route | User type | Page purpose | Existing API endpoint | Method | Current availability | API gap | Security classification | MVP phase |
|---|---|---|---|---:|---|---|---|---:|
| `/fa` | Visitor / reader | Curated home summary, recent changes, featured chart, freshness, and source links | `/api/v1/data-series`; `/api/v1/data-series/{series_id}/latest`; `/api/v1/derived-series/{definition_id}/observations/latest` | GET | Individual reads exist | Curated dashboard configuration and batch-latest read model | Intended public read-only after Stage B gates | 2 |
| `/fa/markets` | Visitor / reader | Curated currencies, commodities, energy, indices, volatility, and financial conditions | `/api/v1/data-series`; `/api/v1/data-series/{series_id}/observations`; `/api/v1/data-series/{series_id}/latest` | GET | Basic catalog and per-series reads exist | Catalog filters, bounded raw ranges, batch latest, curated configuration | Intended public read-only after Stage B gates | 2 |
| `/fa/macro` | Visitor / reader | Curated inflation, rates, labor, growth, liquidity, yield curve, and risk dashboards | `/api/v1/data-series`; `/api/v1/data-series/{series_id}/latest`; `/api/v1/derived-series/{definition_id}/observations` | GET | Raw and derived per-series reads exist | Catalog filters, batch latest, curated configuration | Intended public read-only after Stage B gates | 2 |
| `/fa/indicators` | Visitor / reader | Search and browse the publishable indicator catalog | `/api/v1/data-series`; `/api/v1/data-sources` | GET | Paginated unfiltered lists exist | Search plus code/category/geography/frequency/source/active filters; public-safe catalog | Intended public read-only after Stage B gates | 2 |
| `/fa/indicators/[seriesId]` | Visitor / reader | Series metadata, current value, chart, revisions, as-of view, source, and related Analytics | `/api/v1/data-series/{series_id}`; `/api/v1/data-series/{series_id}/latest`; `/api/v1/data-series/{series_id}/observations`; `/api/v1/data-series/{series_id}/observations/as-of`; `/api/v1/data-series/{series_id}/observations/{observation_id}/revisions`; `/api/v1/data-sources/{source_id}` | GET | Detail, latest, paginated current/as-of observations, revisions, and source exist | Bounded raw date ranges, related-derived-series lookup, methodology content | Intended public read-only after Stage B gates | 3 |
| `/fa/compare` | Research user | Compare at most four raw or derived series with explicit frequency and axis semantics | `/api/v1/data-series/{series_id}/observations`; `/api/v1/derived-series/{definition_id}/observations`; `/api/v1/derived-series/{definition_id}/observations/as-of` | GET | Separate per-series reads exist | Multi-series comparison read model; raw start/end bounds | Private research until Stage B provides a public-safe read model | 4 |
| `/fa/research` | Research user / operator | Configure one of nine transformations, execute a bounded run, inspect output, missingness, metadata, and lineage | `/api/v1/derived-series`; `/api/v1/derived-series/{definition_id}/versions`; `/api/v1/derived-series/{definition_id}/runs`; `/api/v1/analytics-runs/{run_id}`; `/api/v1/analytics-runs/{run_id}/observations`; `/api/v1/analytics-runs/{run_id}/observations/{observation_id}/lineage` | GET, POST | Management, execution, exact output, and lineage exist | Authentication, research/operator roles, CSRF, rate limiting; save/share deferred | Private-only mutation surface | 5 |
| `/fa/methodology` | Visitor / reader | Explain sources, vintages, transformations, missingness, and no-look-ahead behavior | `/api/v1/data-sources`; `/api/v1/data-series/{series_id}` | GET | Structured source and series metadata exist | Editorial methodology/content API | Static reviewed content in MVP; public after Stage B gates | 3 |
| `/fa/about` | Visitor / reader | Explain product principles, limitations, security posture, and contact context | None required for static MVP content | — | Static page can be implemented without backend mutation | General content API deferred | Public static content after Stage B gates | 2 |

## Shared contract notes

- List endpoints use deterministic `limit`/`offset` pagination, with `limit` bounded to
  1–200.
- Raw Macro Data values are exact eight-decimal JSON strings.
- Analytics Decimal outputs are canonical JSON strings.
- UTC is canonical; Persian localization is display-only.
- Current and historical as-of modes must never share an ambiguous visual state.
- GET routes read persisted state and never execute Analytics implicitly.
- Ratio, spread, and other transformations use the explicit Analytics run API.
- Mutation routes are private until secure sessions, CSRF, authorization, and rate
  limiting are implemented.
